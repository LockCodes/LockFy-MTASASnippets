name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install tools
        run: |
          npm install -g @vscode/vsce
          pip install pyyaml requests

      - name: Sync natives
        run: python tools/sync_natives.py

      - name: Generate type definitions
        run: python tools/generate_definitions.py

      - name: Package extension
        run: vsce package

      - name: Create release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            *.vsix
            definitions/mta-sa.lua
          generate_release_notes: true
